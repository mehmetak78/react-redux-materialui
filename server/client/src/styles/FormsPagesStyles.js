
const styles = theme => ({
    appBarSpacer: theme.mixins.toolbar,
    card: {
        minWidth: 275,
        marginTop: 20,
    },
    button: {
        margin: theme.spacing.unit,
    },
});

export default styles;